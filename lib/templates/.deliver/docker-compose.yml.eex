version: '3'

# autogenerated by deli

services:
  edeliver:
    container_name: <%= app %>_edeliver
    build: .deliver
    volumes:
      - .deliver:/deliver
    ports:
      - "<%= docker_port %>:22"
